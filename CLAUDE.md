# CLAUDE.md

The instructions for AI coding agents in this repo live in [`AGENT.md`](./AGENT.md), which is the tool-agnostic source of truth (Claude Code, Cursor, Aider, Continue, etc.).

**Read `AGENT.md` first** — it covers project overview, architecture rules, i18n conventions, Sanity schema patterns, deploy chain, env vars, and the explicit "Do NOT" list.

This file exists only so Claude Code picks up the same context automatically; do not duplicate content here.
